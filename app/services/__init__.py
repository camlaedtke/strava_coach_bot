# This empty file tells Python that `app/services/` is a package.
# Without it, `from app.services.claude import get_claude_reply` would raise
# a ModuleNotFoundError, even if claude.py exists on disk.
